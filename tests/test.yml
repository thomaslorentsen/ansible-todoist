---
- hosts: localhost
  remote_user: root
  tasks:
    - name: install todoist python module
      pip:
        name: todoist

    - name: Test that my module works
      todo:
        api_key: 'foobar'
      register: result
...