---
- hosts: localhost
  remote_user: root
  tasks:
    - name: test that my module works
      todo:
        api_key: 'foobar'
        content: 'This was a test of ansible todoist on travis ci'
      register: result
...
